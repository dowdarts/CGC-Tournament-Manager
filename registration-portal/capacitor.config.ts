import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cgc.tournamentregistration',
  appName: 'Tournament Registration',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
