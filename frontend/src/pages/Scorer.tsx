import React from 'react';

const Scorer: React.FC = () => {
  return (
    <div>
      <h2>Match Score Input (Scorer UI)</h2>
      <p>Input match scores and results</p>
    </div>
  );
};

export default Scorer;
